/**
 * Backward-compatibility alias.
 * All symbols are now provided by the unified fixtures module.
 * Import from './fixtures' in all test files.
 */
export { test, expect, withCDP, type Page, type FrameLocator } from './fixtures';
