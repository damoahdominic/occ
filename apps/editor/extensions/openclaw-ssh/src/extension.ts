import * as vscode from 'vscode';

/**
 * openclaw-ssh — Preview stub.
 *
 * SSH host support is coming soon. This extension activates silently
 * and will register an SSHHostAdapter once the implementation lands.
 */
export function activate(_context: vscode.ExtensionContext): void {
	console.log('[openclaw-ssh] SSH adapter — coming soon');
}

export function deactivate(): void {}
