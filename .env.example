# OCC Free Tier inference backend
# Set these to enable the built-in "OCC Free Tier" option in the editor.
# Without these, users must supply their own API keys (BYOK).
OCC_INFERENCE_ENDPOINT=
OCC_INFERENCE_API_KEY=
